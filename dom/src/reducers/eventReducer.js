import { FETCH_USER, LOGOUT_USER, FETCH_STAFF, GET_SELECTED_TEACHER } from '../types';
import jwt from 'jsonwebtoken';

export default function (state = { statusMsg: '' }, action) {
    switch (action.type) {
        default:
            return state;
    }
}